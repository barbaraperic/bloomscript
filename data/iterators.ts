export const iteratorsData = [
    {
        name: 'forEach',
        code: `const numbers = [1, 2, 3]; 
let sum = 0;

numbers.forEach(number => {
   sum += number
})

console.log(sum) // 6`,
    },
    {
        name: 'map',
        code: `
                        const numbers = [1, 2, 3]; 
                        let sum = 0;

                        numbers.forEach(number => {
                          sum += number
                        })

                        console.log(sum) // 6
                    `,
    },
]
